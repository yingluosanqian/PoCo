# Need

用户希望在群工作区里直接给 bot 发消息就能发起任务。

当前必须点击 `Run Task` 卡片，再在卡片输入框里填 prompt，路径过长，和移动端高频对话式使用习惯不匹配。

用户期望的是：

- 在已绑定 project 的群里，直接发送一条文本消息
- PoCo 默认把这条消息理解为 task prompt
- 不要求用户先输入 `/run`
- 卡片继续承担状态、确认、配置和导航，而不是承担唯一发任务入口
