# Plan

## 范围

- 在 workspace overview card 中加入 `Change Workdir`
- 新增 `workspace_workdir_switcher` 视图
- 新增四个只读目录动作子卡
- 更新测试和最小文档

## 验收标准

- 群里 `workspace.open` 返回的卡片可进入 `Workdir Switcher Card`
- switcher card 展示 current agent、current workdir、source
- 子卡可以返回 switcher card
