# Problem

## 当前状态

- `Choose Preset` 已经有卡片入口
- 但 project 还没有最小 preset 存储与管理动作

## 问题定义

PoCo 当前缺少一条从 DM 管理 preset 到群里应用 preset 的完整最小写链，导致 `Choose Preset` 仍不能成为真实可用的高频路径。

## 不是的问题

- 不是现在就实现完整 preset 排序、删除和权限模型的问题
- 不是现在就实现 recent dirs 自动生成的问题
